---
version: alpha
name: Hermes Logistics
description: >-
  Visual identity for Hermes Logistics — a dark-first, high-contrast logistics
  brand. Deep navy space, an aurora of cyan / amber / sunset accents, and a
  single geometric typeface (Poppins). Source of truth is the "Design system"
  frame (node I5xP6, pages 1.0–2.0) in pencil.pen.

colors:
  # --- Canvas & surfaces (dark mode is the default mode) ---
  background: "#01051D"
  surface: "#001751"
  surface-raised: "#152E6D"
  surface-translucent: "#00175180"
  surface-warm: "#0E0C0A"
  surface-warm-raised: "#201A12"

  # --- Foreground ---
  foreground: "#EBF2FF"
  foreground-muted: "#94A4C2"
  foreground-subtle: "#C5D1E7"
  foreground-warm: "#F4EFE7"
  foreground-warm-muted: "#A69E90"
  on-accent: "#01051D"

  # --- Overlay & borders ---
  surface-overlay: "#0A1740"
  scrim: "#01051DCC"
  border: "#B7CDF5"
  border-hairline: "#D8D0C214"
  border-hairline-cool: "#B7CDF51F"
  border-cool: "#B7CDF547"

  # --- Brand primaries (dark mode) ---
  deep-blue: "#001751"
  blue-sky: "#61F0FF"
  light-yellow: "#FFC152"

  # --- Brand secondaries (dark mode) ---
  sunset-orange: "#FF734D"
  soft-blue: "#EBF2FF"
  turquoise: "#4EC1D2"

  # --- Brand primaries (light mode) ---
  deep-blue-lm: "#031E72"
  blue-sky-lm: "#00DBFF"
  sunset-orange-lm: "#FF501A"
  bright-orange-lm: "#F4AD21"
  soft-blue-lm: "#EBF2FF"

  # --- Neutrals ---
  neutral-900: "#141923"
  neutral-700: "#47546C"
  neutral-500: "#94A4C2"
  neutral-300: "#C5D1E7"
  neutral-000: "#FFFFFF"

  # --- Deep Blue ramp (dark, page 1.4) ---
  deep-blue-100: "#6C8AD0"
  deep-blue-200: "#4E6DB5"
  deep-blue-300: "#2F4D94"
  deep-blue-400: "#152E6D"
  deep-blue-500: "#001751"
  deep-blue-600: "#01051D"

  # --- Light Yellow ramp (dark, page 1.4) ---
  light-yellow-100: "#FFD790"
  light-yellow-200: "#FFC152"
  light-yellow-300: "#CC9B48"
  light-yellow-400: "#99763D"
  light-yellow-500: "#675032"
  light-yellow-600: "#342B27"

  # --- Blue Sky ramp (dark, page 1.4) ---
  blue-sky-100: "#ACF7FF"
  blue-sky-200: "#61F0FF"
  blue-sky-300: "#4EC1D2"
  blue-sky-400: "#3A92A5"
  blue-sky-500: "#286377"
  blue-sky-600: "#14344A"

  # --- Sunset Orange ramp (dark, page 1.4) ---
  sunset-orange-100: "#FF977A"
  sunset-orange-200: "#FF734D"
  sunset-orange-300: "#CC5D44"
  sunset-orange-400: "#99473A"
  sunset-orange-500: "#673130"
  sunset-orange-600: "#341B26"

  # --- Light-mode ramps (page 1.5) ---
  deep-blue-lm-100: "#CDD2E3"
  deep-blue-lm-200: "#9AA5C7"
  deep-blue-lm-300: "#6878AA"
  deep-blue-lm-400: "#354B8E"
  deep-blue-lm-500: "#031E72"
  deep-blue-lm-600: "#01051D"
  sunset-orange-lm-100: "#FFDCD1"
  sunset-orange-lm-200: "#FFB9A3"
  sunset-orange-lm-300: "#FF9676"
  sunset-orange-lm-400: "#FF7348"
  sunset-orange-lm-500: "#FF501A"
  sunset-orange-lm-600: "#C84C25"
  blue-sky-lm-100: "#CCF8FF"
  blue-sky-lm-200: "#99F1FF"
  blue-sky-lm-300: "#66E9FF"
  blue-sky-lm-400: "#33E2FF"
  blue-sky-lm-500: "#00DBFF"
  blue-sky-lm-600: "#00B3D0"
  bright-orange-lm-100: "#FDEFD3"
  bright-orange-lm-200: "#FBDEA6"
  bright-orange-lm-300: "#F8CE7A"
  bright-orange-lm-400: "#F6BD4D"
  bright-orange-lm-500: "#F4AD21"
  bright-orange-lm-600: "#D28C00"

typography:
  display-lg:
    fontFamily: Poppins
    fontSize: 57px
    fontWeight: 700
    lineHeight: 1.2
  display-md:
    fontFamily: Poppins
    fontSize: 45px
    fontWeight: 700
    lineHeight: 1.156
  display-sm:
    fontFamily: Poppins
    fontSize: 36px
    fontWeight: 700
    lineHeight: 1.222
  headline-lg:
    fontFamily: Poppins
    fontSize: 32px
    fontWeight: 500
    lineHeight: 1.25
  headline-md:
    fontFamily: Poppins
    fontSize: 28px
    fontWeight: 500
    lineHeight: 1.286
  headline-sm:
    fontFamily: Poppins
    fontSize: 24px
    fontWeight: 500
    lineHeight: 1.333
  title-lg:
    fontFamily: Poppins
    fontSize: 22px
    fontWeight: 700
    lineHeight: 1.273
  title-md:
    fontFamily: Poppins
    fontSize: 16px
    fontWeight: 700
    lineHeight: 1.5
  title-sm:
    fontFamily: Poppins
    fontSize: 14px
    fontWeight: 700
    lineHeight: 1.429
  body-lg:
    fontFamily: Poppins
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.5
  body-md:
    fontFamily: Poppins
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.429
  body-sm:
    fontFamily: Poppins
    fontSize: 12px
    fontWeight: 400
    lineHeight: 1.333
  label-lg:
    fontFamily: Poppins
    fontSize: 14px
    fontWeight: 500
    lineHeight: 1.429
  label-md:
    fontFamily: Poppins
    fontSize: 12px
    fontWeight: 500
    lineHeight: 1.333
  label-sm:
    fontFamily: Poppins
    fontSize: 11px
    fontWeight: 500
    lineHeight: 1.455
  eyebrow:
    fontFamily: Poppins
    fontSize: 12px
    fontWeight: 700
    lineHeight: 1.5
    letterSpacing: 0.08em
  # --- Navigation scale (responsive; see "Navigation type scale") ---
  # nav-md and nav-sm are aliases of label-lg / label-md at the same values.
  # Only nav-lg introduces a new size, and only for the horizontal nav bar.
  nav-lg:
    fontFamily: Poppins
    fontSize: 16px
    fontWeight: 500
    lineHeight: 1.5
  nav-md:
    fontFamily: Poppins
    fontSize: 14px
    fontWeight: 500
    lineHeight: 1.429
  nav-sm:
    fontFamily: Poppins
    fontSize: 12px
    fontWeight: 500
    lineHeight: 1.333

rounded:
  none: 0px
  xs: 3px
  sm: 4px
  md: 6px
  lg: 8px
  xl: 12px
  2xl: 16px
  full: 9999px

spacing:
  0: 0px
  1: 1px
  2: 2px
  4: 4px
  6: 6px
  8: 8px
  12: 12px
  16: 16px
  20: 20px
  24: 24px
  32: 32px
  40: 40px
  48: 48px
  64: 64px
  80: 80px

components:
  button-primary:
    backgroundColor: "{colors.blue-sky}"
    textColor: "{colors.on-accent}"
    typography: "{typography.label-lg}"
    rounded: "{rounded.md}"
    paddingY: "{spacing.8}"
    paddingX: "{spacing.16}"
    gap: "{spacing.6}"
  button-secondary:
    backgroundColor: "{colors.surface-raised}"
    textColor: "{colors.foreground}"
    typography: "{typography.label-lg}"
    rounded: "{rounded.md}"
    paddingY: "{spacing.8}"
    paddingX: "{spacing.16}"
  button-outline:
    backgroundColor: transparent
    borderColor: "{colors.border-hairline}"
    borderWidth: 1px
    textColor: "{colors.foreground}"
    typography: "{typography.label-lg}"
    rounded: "{rounded.md}"
    paddingY: "{spacing.8}"
    paddingX: "{spacing.16}"
  button-ghost:
    backgroundColor: transparent
    textColor: "{colors.foreground-muted}"
    typography: "{typography.label-lg}"
    rounded: "{rounded.md}"
    paddingY: "{spacing.8}"
    paddingX: "{spacing.16}"
  button-destructive:
    backgroundColor: "{colors.sunset-orange}"
    textColor: "{colors.on-accent}"
    typography: "{typography.label-lg}"
    rounded: "{rounded.md}"
  # Sanctioned exception — marketing navbar CTA only. See "Sanctioned exceptions".
  # It is an OUTLINE button at rest, not a filled one: the orange is the stroke.
  button-cta-marketing:
    backgroundColor: transparent
    borderColor: "{colors.sunset-orange}"
    borderWidth: 1px
    textColor: "{colors.foreground}"
    hoverBackgroundColor: "{colors.sunset-orange}"
    hoverTextColor: "{colors.on-accent}"
    typography: "{typography.label-lg}"
    rounded: "{rounded.full}"
    paddingY: "{spacing.8}"
    paddingX: "{spacing.16}"
    scope: marketing-navbar
  icon-button:
    size: 36px
    rounded: "{rounded.md}"
  icon-button-large:
    size: 40px
    rounded: "{rounded.md}"
  card:
    backgroundColor: "{colors.surface}"
    borderColor: "{colors.border-hairline}"
    borderWidth: 1px
    rounded: "{rounded.lg}"
    padding: "{spacing.24}"
    gap: "{spacing.8}"
  input:
    backgroundColor: "{colors.surface}"
    borderColor: "{colors.border-hairline}"
    borderWidth: 1px
    textColor: "{colors.foreground}"
    placeholderColor: "{colors.foreground-muted}"
    typography: "{typography.body-md}"
    rounded: "{rounded.md}"
    height: 40px
    paddingX: "{spacing.12}"
  input-label:
    typography: "{typography.label-md}"
    textColor: "{colors.foreground-muted}"
    gap: "{spacing.8}"
  badge:
    typography: "{typography.label-sm}"
    rounded: "{rounded.full}"
    paddingY: "{spacing.2}"
    paddingX: "{spacing.8}"
    height: 20px
  tab-item:
    typography: "{typography.label-lg}"
    rounded: "{rounded.md}"
    height: 32px
    activeBackgroundColor: "{colors.surface-raised}"
    inactiveTextColor: "{colors.foreground-muted}"
  sidebar:
    width: 256px
    backgroundColor: "{colors.surface-warm-raised}"
    borderColor: "{colors.border-hairline}"
    padding: "{spacing.16}"
    itemGap: "{spacing.2}"
    itemHeight: 32px
    itemRounded: "{rounded.md}"
  sidebar-rail:
    width: 72px
  topbar:
    height: 64px
    paddingX: "{spacing.32}"
    gap: "{spacing.16}"
  # Marketing landing navigation bar. Distinct from `topbar`, which is the
  # product app shell. Breakpoint-indexed values are ordered
  # 390 / 768 / 844 / 1024 / 1440.
  navbar:
    height: [64, 80, 80, 80, 80]
    paddingX:
      ["{spacing.20}", "{spacing.32}", "{spacing.32}", "{spacing.48}", "{spacing.80}"]
    backgroundColor: "{colors.surface-translucent}"
    scrolledBackgroundColor: "{colors.surface}"
    borderBottomColor: "{colors.border-hairline-cool}"
    borderBottomWidth: 1px
    logoWidth: [130, 150, 150, 190, 238]
    logoAspectRatio: 4.409
    linkTypography:
      [null, "{typography.nav-sm}", "{typography.nav-md}", "{typography.nav-md}", "{typography.nav-lg}"]
    linkColor: "{colors.foreground}"
    linkHoverColor: "{colors.blue-sky}"
    linkActiveColor: "{colors.blue-sky}"
    focusRingColor: "{colors.blue-sky}"
    focusRingWidth: 2px
    focusRingOffset: "{spacing.2}"
    linkGap: [null, "{spacing.16}", "{spacing.16}", "{spacing.24}", "{spacing.32}"]
    groupGap: ["{spacing.16}", "{spacing.16}", "{spacing.16}", "{spacing.24}", "{spacing.32}"]
    ctaPaddingX:
      [null, "{spacing.16}", "{spacing.16}", "{spacing.20}", "{spacing.24}"]
    menuIconSize: 24px
  # Route-level entry point to the web app (`/signup`). A LINK, not a button:
  # the bar carries exactly one CTA (`button-cta-marketing`), and this is not a
  # second one. See "Sign-up entry point". Breakpoint-indexed values are
  # ordered 390 / 768 / 844 / 1024 / 1440.
  navbar-signup-link:
    element: link
    href: /signup
    # The same English string in both locales — Title Case, matching `nav.cta`.
    # A deliberate loanword in Spanish, not a missing translation, and the
    # string the threshold below is measured against. See "Sign-up entry point"
    # before changing it.
    label: "Sign Up"
    # Inherits the nav link scale verbatim: nav-md from 1280, nav-lg at 1440,
    # nav-md inside the drawer's action group at 390. The three nulls carry the
    # same meaning they carry in `navbar.linkTypography` at 390 — the element is
    # not on the bar at that width. Resolved, not pending.
    typography: ["{typography.nav-md}", null, null, null, "{typography.nav-lg}"]
    textColor: "{colors.foreground}"
    hoverTextColor: "{colors.blue-sky}"
    backgroundColor: transparent
    borderWidth: 0
    focusRingColor: "{colors.blue-sky}"
    focusRingWidth: 2px
    focusRingOffset: "{spacing.2}"
    ariaCurrent: none
    # Measured, and re-measured after the label shortened to "Sign Up": the
    # binding constraint is now the Spanish section links plus `nav.cta`, not
    # this label. Spanish clears at 1028px, which is not a breakpoint; 1024
    # leaves the row 4px over. 1280 is the lowest stop that fits both locales.
    barVisibleFrom: 1280px
    barSlot: right-cluster-first
    drawerGroup: action
    drawerOrder: first
  navbar-lang-chip:
    element: link
    rounded: "{rounded.sm}"
    typography:
      ["{typography.nav-sm}", "{typography.nav-sm}", "{typography.nav-sm}", "{typography.nav-md}", "{typography.nav-md}"]
    paddingY: "{spacing.4}"
    paddingX:
      ["{spacing.12}", "{spacing.8}", "{spacing.8}", "{spacing.12}", "{spacing.12}"]
    gap: "{spacing.8}"
    activeBackgroundColor: "{colors.deep-blue-100}"
    activeTextColor: "{colors.on-accent}"
    inactiveBackgroundColor: transparent
    inactiveTextColor: "{colors.foreground-muted}"
    inactiveHoverTextColor: "{colors.foreground}"
    focusRingColor: "{colors.blue-sky}"
    focusRingWidth: 2px
    focusRingOffset: "{spacing.2}"
  navbar-drawer:
    backgroundColor: "{colors.surface}"
    scrimColor: "{colors.scrim}"
    rounded: "{rounded.2xl}"
    roundedTop: "{rounded.none}"
    borderColor: "{colors.border-hairline-cool}"
    borderWidth: 1px
    padding: "{spacing.20}"
    groupGap: "{spacing.24}"
    itemHeight: 44px
    itemGap: "{spacing.4}"
    linkTypography: "{typography.nav-lg}"
    ctaTypography: "{typography.nav-md}"
    utilityTypography: "{typography.nav-sm}"
  content-area:
    padding: "{spacing.32}"
    gap: "{spacing.16}"
  table-row:
    height: 44px
    borderColor: "{colors.border-hairline}"
    typography: "{typography.body-md}"
  table-header:
    height: 40px
    typography: "{typography.label-md}"
    textColor: "{colors.foreground-muted}"
  modal:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.xl}"
    padding: "{spacing.24}"
    gap: "{spacing.16}"
  tooltip:
    backgroundColor: "{colors.surface-overlay}"
    borderColor: "{colors.border-cool}"
    borderWidth: 1px
    textColor: "{colors.foreground}"
    fontSize: 13px
    rounded: "{rounded.md}"
    paddingY: "{spacing.6}"
    paddingX: "{spacing.12}"
---

# Hermes Logistics — DESIGN.md

## Overview

Hermes is a logistics platform that has to feel **precise, nocturnal and in
motion**. The identity is dark-first: a near-black navy void (`background`,
`#01051D`) that reads like a night map, lit by a small number of saturated
signal colors — cyan, amber, sunset orange — that behave like beacons, not
decoration.

Three principles drive every decision:

1. **Darkness is the substrate, light is the signal.** Dark mode is the primary
   mode. Light mode exists (page 1.1 / 1.5) but is the derived variant, with its
   own deeper primaries so contrast survives the inversion.
2. **One typeface, many voices.** Poppins carries the entire system. Hierarchy
   is expressed through size and weight on a Material-style five-tier scale,
   never through a second family.
3. **Accents are rationed.** A screen shows one dominant accent. Cyan, amber and
   orange are peers, not a gradient set — mixing all three in one component is
   noise, not brand.

Two surface families coexist and must not be blended inside one screen:

- **Cool / Navy** (`background`, `surface`, `foreground`) — marketing landing,
  onboarding, auth. This is the brand default.
- **Warm / Bronze** (`surface-warm` `#0E0C0A`, `surface-warm-raised` `#201A12`,
  `foreground-warm` `#F4EFE7`) — the operations dashboard, where dense data sits
  for long sessions and pure navy would vibrate against the map tiles.

Canonical source: the `Design system` frame (node `I5xP6`) in `pencil.pen`,
pages **1.0** palette → **1.1** light palette → **1.2/1.3** usage proportion →
**1.4/1.5** shades → **1.6** contrast → **1.7** logotype → **2.0** typography.
Read it with the pencil MCP tools; never with `Read`/`Grep`.

## Colors

### Roles

| Role | Token | Value | What it is for |
| --- | --- | --- | --- |
| Canvas | `background` | `#01051D` | The page itself. Never used on text. |
| Surface | `surface` | `#001751` | Cards, panels, elevated blocks on the canvas. |
| Raised surface | `surface-raised` | `#152E6D` | Hover, selected rows, nested panels, secondary buttons. |
| Translucent surface | `surface-translucent` | `#00175180` | `surface` at 50%, for a sticky bar sitting *over* scrolling content and paired with a backdrop blur. The one sanctioned use of alpha on a brand fill; it is a scrim, not a tint. Never use it as a flat fill on an opaque parent — there, use `surface`. |
| Primary accent | `blue-sky` | `#61F0FF` | The single most important action or datum on a screen. |
| Warning / value accent | `light-yellow` | `#FFC152` | Attention states, pricing highlights, in-transit. |
| Alert accent | `sunset-orange` | `#FF734D` | Destructive actions, delays, exceptions. |
| Supporting accent | `turquoise` | `#4EC1D2` | Charts and secondary data series only. |
| Body text | `foreground` | `#EBF2FF` | All primary copy on dark surfaces. |
| Secondary text | `foreground-muted` | `#94A4C2` | Labels, captions, placeholder, disabled. |
| Hairline (warm) | `border-hairline` | `#D8D0C214` | 1px separators on the warm bronze dashboard. |
| Hairline (cool) | `border-hairline-cool` | `#B7CDF51F` | 1px separators on the cool navy surfaces. |
| Scrim | `scrim` | `#01051DCC` | The dimmed field behind a modal, sheet or drawer. |

Borders are whispers, never lines — but a whisper has to be the right
temperature. `border-hairline` is a **warm** cream (`#D8D0C2`) and belongs to the
Warm / Bronze family; laid over navy it reads as a dirty, yellowed edge.
`border-hairline-cool` is the same `#B7CDF5` base as `border-cool`, dropped from
28% to 12% alpha. Use the cool hairline on `background` / `surface` /
`surface-translucent`, the warm one on `surface-warm*`. Picking the hairline is
the same decision as picking the surface family — they travel together.

A surface gets **one** hairline value, not one per state. A bar that changes its
alpha on scroll changes its *fill*; the border stays put. Two different border
alphas for the scrolled and unscrolled state of the same element is an invented
intermediate step, and the ramps exist precisely so that does not happen.

Light mode swaps the primaries for their deeper LM variants —
`deep-blue-lm` `#031E72`, `blue-sky-lm` `#00DBFF`, `sunset-orange-lm` `#FF501A`,
`bright-orange-lm` `#F4AD21` — over a `#FFFFFF` / `soft-blue-lm` `#EBF2FF`
canvas with `neutral-900` `#141923` text. Do not reuse the dark-mode hexes on a
light canvas: `#61F0FF` on white fails every contrast target.

### Proportion

Pages 1.2 and 1.3 fix the mix. Read it as roughly **60 / 30 / 10**:

- **~60%** dominant base — `deep-blue` in dark mode, `soft-blue-lm` in light.
- **~30%** foreground and neutrals — text, icons, hairlines.
- **~10%** accents, split evenly between cyan, amber and orange. The three
  accents get *equal* share; none is allowed to become the de-facto background.

### Ramps

Each brand color has a six-step ramp (pages 1.4 / 1.5) running from a near-black
tint at `-600` to a pale tint at `-100`. Use `-500`/`-200` as the "true" brand
value, `-600`/`-500` for tinted backgrounds behind an accent (badge fills, chart
bands), and `-100`/`-200` for text or icons sitting *on* those tinted
backgrounds. Never invent an intermediate step — the ramps exist so that
generated UI lands on the same values twice.

### Contrast

Page 1.6 is the accessibility contract. Every accent surface has a pre-approved
text color; agents must pick from this table rather than compute their own.

| Accent surface | Text | Rating |
| --- | --- | --- |
| `#001751` `#152E6D` `#2F4D94` Deep Blue | `#EBF2FF` | AAA |
| `#4E6DB5` Deep Blue | `#FFFFFF` | AA |
| `#6C8AD0` Deep Blue | `#01051D` | AA |
| `#FFD790` `#FFC152` `#CC9B48` Light Yellow | `#01051D` | AAA |
| `#675032` Light Yellow | `#EBF2FF` | AA |
| `#342B27` Light Yellow | `#EBF2FF` | AAA |
| `#ACF7FF` `#61F0FF` `#4EC1D2` Blue Sky | `#01051D` | AAA |
| `#286377` Blue Sky | `#EBF2FF` | AA |
| `#14344A` Blue Sky | `#EBF2FF` | AAA |
| `#FF977A` `#FF734D` Sunset Orange | `#01051D` | AAA |
| `#CC5D44` Sunset Orange | `#01051D` | AA |
| `#673130` `#341B26` Sunset Orange | `#EBF2FF` | AAA |

Rules that follow from it: **the three bright accents always carry dark text**
(`#01051D`), never white. Body copy must clear WCAG AA (4.5:1); text at
`title-lg` and above may sit at AA large (3:1) but should still target AA.

## Typography

Poppins, single family. The served set is 400 / 500 / 700 from `public/fonts/`
plus a temporary 600 shim — see Sanctioned exceptions.
The scale is Material-style with five tiers (page 2.0). The 500 face was missing
from the build for a long time, which silently flattened the Headline and Label
tiers into Body; that is now resolved — see
[Resolved](#resolved--the-font-payload-is-amended-to-match-the-scale).

| Tier | Token | Size / Line height | Weight | Use |
| --- | --- | --- | --- | --- |
| Display | `display-lg` / `md` / `sm` | 57/68 · 45/52 · 36/44 | 700 | Hero statements only — one per screen. |
| Headline | `headline-lg` / `md` / `sm` | 32/40 · 28/36 · 24/32 | 500 | Section openers on marketing and onboarding. |
| Title | `title-lg` / `md` / `sm` | 22/28 · 16/24 · 14/20 | 700 | Card titles, modal titles, table group headers. |
| Body | `body-lg` / `md` / `sm` | 16/24 · 14/20 · 12/16 | 400 | Paragraphs, table cells, descriptions. |
| Label | `label-lg` / `md` / `sm` | 14/20 · 12/16 · 11/16 | 500 | Buttons, form labels, badges, nav items. |

- **Weight carries meaning.** Display and Title are Bold (700); Headline and
  Label are Medium (500); Body is Regular (400). Do not bold body copy to create
  emphasis — promote it to `title-sm` or change its color instead.
- `body-md` (14px) is the workhorse for product UI; `body-lg` (16px) for
  marketing prose. Never go below `label-sm` (11px).
- `eyebrow` is `label-md` in Bold with `0.08em` tracking, uppercase — used for
  section numbering and category kickers.
- Measure: cap paragraphs at ~72 characters. On the landing page that means a
  fixed-width text box, not a full-bleed one.

### Navigation type scale

The landing navigation bar is the one place where type size is a function of
viewport width rather than of role. A nav link has a fixed job at every width,
so it cannot move tiers as it shrinks — it stays a Label and only changes size:

| Viewport | Token | Size / Weight |
| --- | --- | --- |
| 1440 | `nav-lg` | 16 / 500 |
| 1024 | `nav-md` | 14 / 500 |
| 844 | `nav-md` | 14 / 500 |
| 768 | `nav-sm` | 12 / 500 |
| 390 | `nav-lg` in the drawer | 16 / 500 |

`nav-md` and `nav-sm` are the same values as `label-lg` and `label-md`; they are
named separately only so the responsive step is legible in one place. **`nav-lg`
(16/500) is the single new value** this scale introduces — 16px already exists in
the scale at 400 (`body-lg`) and 700 (`title-md`), and the nav needs the Medium
weight between them.

Only 16, 14 and 12 are legal in navigation. **13 and 15 are not steps on any
Hermes scale**; if a bar does not fit, change the padding, the gap or the number
of links — never split the type scale to buy 20px.

At 390 the links leave the bar entirely and move into the drawer, where there is
vertical room, so they go back up to 16 rather than down. Size follows available
space, not device class.

Weight is **500 across every nav item, active and inactive alike.** Weight does
not encode state anywhere in this system: an active nav item is marked with
`blue-sky` `#61F0FF` plus `aria-current`, never with a heavier face. A 600 nav
link is a violation twice over — it invents a state signal and it uses a weight
that is not on the scale.

## Layout

- **Breakpoints / artboards:** 390 (mobile), 768 (tablet portrait), 844 (tablet
  landscape), 1024 (small desktop), 1440 (desktop). Every landing section exists
  at all five widths in `pencil.pen`; a new section is not done until it does.
- **Spacing scale:** 4px base — 2, 4, 6, 8, 12, 16, 20, 24, 32, 40, 48, 64, 80.
  Values off this scale are a bug. 8 and 16 do most of the work; 32 separates
  major regions; 64/80 separate landing sections.
- **App shell:** sidebar `256px` (collapsed rail `72px`) + main `1184px` at a
  1440 viewport. Topbar is `64px` tall with `0 32px` padding. The content region
  uses `32px` side/bottom padding and a `16px` vertical gap between blocks.
- **Sidebar internals:** `16px` frame padding, `2px` gap between nav items,
  `32px` item height, `8px` item radius.
- **Marketing rhythm:** sections are full-bleed at the artboard width with an
  internal max content column; vertical padding of 64–80px top and bottom.
- Flexbox only — single-axis. Grid-like arrangements are built as explicit row
  frames, and children use `fill_container` rather than repeated pixel widths.

## Elevation & Depth

Depth is **tonal and atmospheric**, not shadow-driven.

1. **Tonal layers.** Lift is a step up the Deep Blue ramp:
   `background #01051D` → `surface #001751` → `surface-raised #152E6D`. On the
   warm dashboard: `#0E0C0A` → `#15120D` → `#201A12`.
2. **Hairline borders.** A 1px `#D8D0C214` stroke defines an edge where two
   tonal layers are too close to read. Borders are always 1px, always
   inner-aligned, never a saturated color.
3. **Ambient lights.** Dashboard and hero canvases carry an absolutely
   positioned `Ambient Lights` group: 680–820px radial-gradient ellipses in
   cyan (`#61F0FF24`) and warm orange (`#FF9D5C3D`) fading to fully transparent,
   with a 64–72px blur. They sit behind content, never over it, and are the only
   sanctioned "glow".
4. **Shadows are minimal.** At most a `0 1px 1.75px #0000000d` outer shadow on
   cards. No colored shadows, no multi-layer drop shadows, no neon outer glow on
   text or buttons.

Order of preference when something must come forward: tone → hairline → ambient
light → shadow. If the first three do the job, skip the shadow.

## Shapes

Radii are small and consistent — the brand reads engineered, not soft.

| Token | Value | Applied to |
| --- | --- | --- |
| `xs` / `sm` | 3–4px | Chips, tags, tiny indicators, chart bars. |
| `md` | 6px | Buttons, inputs, selects, menu items, tooltips. |
| `lg` | 8px | Cards, table containers, sidebar items, dropdowns. |
| `xl` / `2xl` | 12–16px | Modals, sheets, large feature panels, hero media. |
| `full` | 9999px | Pills, badges, avatars, toggle tracks, status dots. |

One radius per element — never mix corner values on a single frame. Container
and its children should differ by one step at most (an 8px card holding 6px
buttons). Illustrations and the logo mark are the only places freeform curves
appear; everything structural is a rounded rectangle or an ellipse.

## Components

The `pencil.pen` library is shadcn-derived. Reusable components live at the top
of the canvas under `K:*` ids (`K:VSnC2` Button/Default, `K:pcGlv` Card,
`K:1415a` Input Group/Default, `K:bG7YL` Table, `K:PV1ln` Sidebar, …) and expose
shadcn semantic variables (`K:--primary`, `K:--card`, `K:--border`) themed by
the `K:Mode = Dark` axis. **Instance these components; do not redraw them.**

Two overrides always apply when instancing them into a Hermes screen: the
primitives ship with shadcn's **Inter** default and neutral `K:--*` colors —
override the text to **Poppins** and re-point fills to the Hermes tokens above.
The geometry (heights, radii, padding) is already correct; leave it alone.

- **Buttons.** Height 36 (default) / 40 (large), 6px radius, `8px 16px` padding,
  `6px` gap between icon and label, label at `label-lg`. Variants: Default
  (accent fill, dark label), Secondary (raised surface), Outline (hairline
  border, transparent fill), Ghost (muted label, no chrome), Destructive
  (`sunset-orange` fill, dark label). One Default button per view. The landing
  navbar CTA is the one sanctioned departure — see Sanctioned exceptions.
- **Icon buttons.** Square, 36 or 40px, same radius and variants.
- **Cards.** `surface` fill, 1px hairline, 8px radius, header/content/actions
  stacked with `24px` padding and `8px` internal gap.
- **Inputs.** 40px tall, 6px radius, hairline border, `12px` horizontal padding,
  value at `body-md`, placeholder at `foreground-muted`. The label sits above at
  `label-md` with an `8px` gap; helper and error text below at `body-sm`, error
  in `sunset-orange`. Focus is a `ring` outline, not a glow.
- **Badges.** 20px tall, pill radius, `label-sm`. Status mapping is fixed:
  delivered/ok → `blue-sky`, in transit/pending → `light-yellow`, delayed or
  failed → `sunset-orange`, neutral/draft → `surface-raised` with muted text.
- **Tables.** 44px rows, 40px headers, hairline row separators, no zebra
  striping, no vertical rules. Headers are `label-md` in `foreground-muted`;
  numeric columns are right-aligned.
- **Sidebar.** 256px, warm gradient fill, 32px items at 8px radius; the active
  item is a filled `surface-warm-raised` block with full-opacity text, inactive
  items are muted with no chrome.
- **Modals & sheets.** 12–16px radius, `24px` padding, `16px` gap, over a scrim.
  Title at `title-lg`, body at `body-md`, actions right-aligned with the Default
  button last.
- **Tooltips.** `surface-overlay` `#0A1740` fill with a `border-cool` `#B7CDF547`
  1px border, 6px radius, `6px 12px` padding, 13px text, wrapped at ~220px.
- **Logotype.** The logo is **vector, always** — in every medium, without
  exception. See the Logotype section below for where the canonical vector
  lives and which variant to use.

## Navigation

The landing navigation bar is its own component (`components.navbar`), not a
reskinned `topbar` — `topbar` is the product app shell and stays 64px at every
width. All breakpoint-indexed values below are ordered 390 / 768 / 844 / 1024 /
1440, matching the five artboards in Layout.

- **Bar.** Height 64 below 768, **80 from 768 up**. Side padding
  20 / 32 / 32 / 48 / 80. Fill `surface-translucent` `#00175180` at rest over the
  hero, `surface` `#001751` once scrolled. One bottom border for both states:
  1px `border-hairline-cool`.
- **Logo.** Width 130 / 150 / 150 / 190 / 238, height derived from the 4.409:1
  aspect (so 29.5 / 34 / 34 / 43 / 54 — **not** 130×30, which stretches it 1.7%).
- **Links.** `nav-sm` / `nav-md` / `nav-md` / `nav-lg` per the Navigation type
  scale; `foreground` `#EBF2FF`, hover and active `blue-sky` `#61F0FF`, active
  additionally carrying `aria-current`. Focus is a 2px `blue-sky` ring at a
  `2px` offset. Gap 16 / 16 / 24 / 32. Below 768 they move into the drawer.
- **CTA.** `button-cta-marketing` — the sanctioned exception. Padding
  `8px 16/16/20/24`, which lands the control at the 36px default button height.
  One per bar. It stays the bar's primary action; the sign-up route is a link
  beside it, not a competing button — see
  [Sign-up entry point](#sign-up-entry-point).
- **Sign-up link.** `navbar-signup-link`, first item of the right cluster from
  1280 up and in the drawer's action group below 768. Label `"Sign Up"`, the
  same English string in both locales — see [Sign-up entry point](#sign-up-entry-point).
- **Menu icon.** 24px glyph (`spacing.24`), `foreground`.
- **Link semantics are not recordable on the canvas today.** Which nav items are
  real links — and where they point — is part of the design, not an
  implementation detail: it is the whole difference between the locale switcher
  and a pair of buttons. The pen cannot currently hold it. `TextStyle.href` is
  in the v2.17 schema but the running pen.dev build discards it on both `Insert`
  and `Update`, and `metadata` is write-once at `Insert`, so it cannot be added
  to nodes that already exist. Recreating a node to gain the field changes its
  id and breaks every reference to it, which costs more than the annotation is
  worth. **Until the runtime persists `href`, this document is the record**: the
  four nav items point at `#who-we-are` / `#benefits` / `#features` /
  `#pricing`, the locale chips at `/` and `/es`, and the navbar CTA at nothing —
  it opens a modal. Do not infer from a pen node's silence that it is a button.
- **844 is a real breakpoint, not a copy of 768.** Mobile landscape has 76px more
  width than tablet portrait; it spends them on the larger `nav-md` links and on
  air between the logo and the link group. A 768 layout pasted into an 844 frame
  is an unfinished screen, not a responsive one.

### Sign-up entry point

The bar carries one route out of the landing and into the web app: `/signup`.
It is a **third category**, and the distinction is the whole ruling:

1. the four section links are in-page anchors with scroll-spy and `aria-current`;
2. `button-cta-marketing` is the single conversion CTA;
3. `navbar-signup-link` is a **route-level text link** — a door, not a call.

**Treatment.** Exactly the nav link tokens, no more: `navbar.linkColor`
`foreground` `#EBF2FF` at rest, `navbar.linkHoverColor` `blue-sky` `#61F0FF` on
hover, weight 500, the nav type scale, and the same 2px `blue-sky` focus ring at
a `2px` offset. **No fill, no border, no radius, and no `aria-current`** — it
points at another document, so there is no current-section state to express.
This introduces zero new tokens.

**Why it is not a button.** Three treatments were priced and each fails a rule
already in this document:

- `button-primary` (`blue-sky` fill) — cyan is the landing's *state* colour: it
  is what marks a hovered and an active nav link. A saturated cyan block in the
  same bar collides with that signal, breaks "one Default button per view", and
  demotes the sanctioned CTA. Promoting the sign-up link to primary is therefore
  not a styling choice; it requires rewriting
  [Sanctioned exceptions](#marketing-navbar-cta--button-cta-marketing) first.
- `button-secondary` (`surface-raised` `#152E6D`) — **1.32:1** against the
  at-rest bar, already measured in
  [Language switcher](#language-switcher). A state-bearing surface under 3:1
  fails WCAG 1.4.11.
- `button-outline` (`border-hairline`) — the warm hairline is forbidden on navy,
  and `border-hairline-cool` at 12% alpha is an invisible boundary sitting
  beside a 1px orange pill: it reads as a broken or disabled twin of the CTA.

**Placement.** Desktop: first item of the right cluster, so the bar reads
`logo · links · sign-up · CTA · locale` — ascending commitment left to right,
with the plain link adjacent to the pill that outranks it. It is **not** a fifth
entry in the section-anchor list. Drawer: the **action** group, above the CTA,
at `navbar-drawer.itemHeight` 44 with `itemGap` 4. The drawer keeps **three**
groups and the `24 + 1 + 24` rhythm untouched; only the action group grows, by
**48px** (measured). A fourth group is refused: it would add a fourth hairline
and a fourth type step below `nav-sm`, and no such step exists.

Within the action group both items are `nav-md`, so size cannot rank them.
Order and chrome do, and the primary goes **last** — the same rule modals use
for their Default action.

**The label is `"Sign Up"`, Title Case, identical in both locales.** The English
string in Spanish is a deliberate loanword — the register of *login* / *logout*,
which Spanish technical usage has absorbed — and a user decision, not a missing
translation. Do not "fix" it back to `"Registrarse"`: that label renders 68.3 /
79.7 / 91.1px at 12 / 14 / 16px against `"Sign Up"`'s 45.9 / 53.5 / 61.2, and
every number in the table below is measured against the short string. Title
Case rather than `"Sign up"`, because the bar's only other multi-word label is
`nav.cta` — `"Talk to Sales"` / `"Hablar con Ventas"` — and one sentence-case
item in a Title Case row is drift, not restraint.

**`barVisibleFrom: 1280px`, re-measured after the label shortened, and it
stands.** Free slack in the bar row once the existing gaps are paid for,
production build, scrollbar excluded, both locales. Negative = the row exceeds
its content box.

| Viewport | EN slack | ES slack | Verdict |
| --- | --- | --- | --- |
| 390 | in the drawer | in the drawer | n/a — not on the bar |
| 768 | **+21** | **−64** | fails ES; three elements leave the viewport |
| 843 | +96 | **+11** | fits both — but 843 is not a stop, and 844 takes it back |
| 844 | +56 | **−35** | fails ES; three elements leave the viewport |
| 1024 | +87 | **−4** | fails ES by 4px; nothing leaves the viewport, no scroll |
| 1280 | ample | **+92** | **fits both** |
| 1440 | +304 | **+206** | **fits both** |

The Spanish clearing point, by bisection, is **1028px**: 4px short at 1024, in
from 1028, and the layout is constant from `lg` to `xl`, so slack grows 1px per
px of viewport across that whole band. Spanish also clears inside the 12px band
from roughly 832, but 844 steps the nav type to 14 and takes it straight back —
1028 is the only durable one, and it is not a breakpoint. Adopting it would add a
sixth width to [Layout](#layout) whose sole justification is the rendered width
of one Spanish string; a single copy edit would move it, in a rule nobody reads
while editing copy. The threshold therefore has to land on a stop the bar is
already designed at: 768, 1024 or 1280.

**What changed is the reason, not the value.** The sign-up label is no longer the
binding constraint. In Spanish the space goes to the four section links (283px at
768) and above all to `nav.cta` `"Hablar con Ventas"` (~190px of the right
cluster's 249). The Spanish bar at 768 **already** exceeded its 704px content box
by 2px before anything was added to it, surviving only by pinning the group gap
to 12px below the 16px `navbar.groupGap` fixes there — a pre-existing defect, and
the reason 768 is not a candidate at any label length.

**Why not 1024.** Spanish lands at −4. That is the same category as the 768
defect above, which is exactly why it is refused rather than excused: one
documented defect does not license a second, and a row shipped at −4 carries zero
headroom, so any one of six labels gaining a word re-breaks it. The 4px are
**not** to be recovered from `navbar.groupGap` at `lg` (24 → 20). 20 is on the
spacing scale, but it would put the gap *between* groups below `navbar.linkGap`
24, the gap *inside* the link group. Grouping on this bar is carried by spacing
alone — there are no rules, no fills, no chrome separating logo · links · cluster
— so inverting the two gaps makes the bar read as five loose items. Refused.

**Why not 768, with or without a shorter Spanish CTA.** Freeing the ~64px needed
there means cutting `nav.cta`, and that inverts the bar's own ranking:
[Sanctioned exceptions](#marketing-navbar-cta--button-cta-marketing) fixes the
CTA as the bar's primary action, with the sign-up route as "a link beside it, not
a competing button". Degrading the single conversion label so a route-level door
can appear one stop earlier is that rule read backwards. And the Spanish forms
short enough to pay 64px — `"Contactar"`, `"Ventas"` — are generic where the
current label names both the action and the counterpart; the tone rule outranks
the 64px. Refused at the source, not costed.

So the link is on the bar from 1280 up and in the drawer below 768, **in both
locales identically**. An element that exists in English and not in Spanish is
not a responsive design, it is an accident; and a bar that only fits by
shrinking type is covered by the standing rule that a bar which only fits at
13px does not fit.

**Why not a per-locale threshold.** A `html[lang="es"]` variant putting the link
on the bar at 768 in English and at 1280 in Spanish is refused by the rule just
stated, and it would also bind a breakpoint to the string table: the geometry of
the bar would then be owned by whoever edits copy. The threshold is one number,
in both locales, measured against the longer locale.

**The typography nulls are resolved, not pending.**
`navbar-signup-link.typography` is `nav-md` at 390 (the drawer's action group),
null at 768 / 844 / 1024, and `nav-lg` at 1440, with `nav-md` from 1280 — the
five-width index has no 1280 column, so the token comment carries that value. The
nulls now mean exactly what they mean in `navbar.linkTypography` at 390: **the
element is not on the bar at that width.** They no longer stand in for an open
question.

**Open — 768 to 1279 still has no bar door, and it is now purely geometry.**
English would fit there from 768, Spanish not until 1028, so the band has no
entry point into the web app in either locale. The honest options are unchanged:
extend the drawer's range from `< 768` to `< 1280`, which is what "size follows
available space, not device class" prescribes but which rewrites the 768 / 844 /
1024 artboards; or serve those widths from an in-page entry point and leave the
bar without one. **Neither is applied here** — it is a decision for the user. What
changed is that no label can close it: the deficit lives in the section links and
the CTA, so this has stopped being a copy question.

**`navbar-signup-link` is not in the pen.** This document is ahead of frame
`I5xP6` for this one element, in the same way it is already the sole record of
which nav items are real links. The pen owes the 1440 bar variant and the
two-row action group in `nyRec → KCMkq`.

### Measuring contrast on the bar

The bar has **two** fills — `surface-translucent` `#00175180` over the hero at
rest, and `surface` `#001751` once scrolled — so "contrast against the bar" is
ambiguous unless the state is named. **It is always measured against the at-rest
translucent state, because that is the worse case.** Compositing 50% navy over
the hero *raises* the surround's luminance to roughly `#152258`, which costs
about 0.4 of a contrast point against it; a value tuned against the solid
`#001751` can clear 3:1 there and still fail in the state the user sees first.

This applies to every control that lands on the bar — chips, badges, pills,
toggles, any element whose own fill has to be distinguishable — not only to the
locale switcher. Measure on real composited pixels in the unscrolled state; do
not compute against `#001751` and assume the scrolled figure covers both.

### Language switcher

**The switcher is a pair of links, not a pair of buttons.** Each locale is its
own indexable URL — `/` serves English, `/es` serves Spanish — so the control
has to be a real `<a href>`: crawlable, openable in a new tab, and functional
without JavaScript. A button that swaps the locale in place is not an
alternative implementation of this component; it is a different, worse one, and
it is what made the Spanish landing invisible to search engines.

Each chip carries `hreflang` and `lang` for the locale it points at, and the
current one is marked with `aria-current="page"`. Nothing about the visual
treatment changes — the chip geometry, fill and label colors below are
unchanged — but two consequences follow from being a link:

- **State is not a pressed state.** `aria-pressed` and `aria-selected` do not
  apply. The current locale is expressed with the active fill plus
  `aria-current="page"`, exactly as an active nav link is expressed with
  `blue-sky` plus `aria-current`.
- **Focus must be visible.** A link is in the tab order at every width,
  including inside the drawer. Focus is a 2px `blue-sky` `#61F0FF` ring at a
  `2px` offset — the same ring the nav links use — never a glow, never a
  background change, and never removed.

The active-locale chip marks state with a **surface**, which makes it a UI
component boundary: WCAG 1.4.11 requires **3:1** against what surrounds it, and
that is not negotiable by taste.

- **Active:** `deep-blue-100` `#6C8AD0` fill, `on-accent` `#01051D` label.
  **4.41:1** against the at-rest translucent bar and 4.98:1 once scrolled, so it
  clears 1.4.11 in both states; the pair is already pre-approved AA in the
  contrast table (5.94:1 on the label).
- **Inactive:** no fill, `foreground-muted` `#94A4C2` label — 6.73:1, and
  `foreground-muted` is the contract floor for metadata.
- **Inactive hover:** the label rises to `foreground` `#EBF2FF`. The fill stays
  transparent — hover must not borrow the active chip's surface, or the two
  states become indistinguishable mid-gesture.
- `sm` (4px) radius, `4px` vertical padding, `8px` horizontal at 768/844 and
  `12px` at 390 and from 1024, `8px` between the two chips. The switcher is the
  one navbar element that survives into the drawer unchanged, so 390 has a
  value here where `navbar.linkTypography` and `navbar.ctaPaddingX` are null.
- Type is `nav-sm` (12) at 390/768/844 and `nav-md` (14) from 1024, at **weight
  500** like every other nav item. A chip left at 400 is Body weight on a Label
  control and reads as prose next to the links beside it.

A `#FFFFFF1A` chip (white at 10%) composites to `#1A2E62` and gives **1.30:1**,
failing outright. Note that the obvious alternative, `surface-raised` `#152E6D`,
only reaches **1.32:1** and fails too — the fix had to come from further up the
Deep Blue ramp, and `deep-blue-300` `#2F4D94` (2.10:1) is still not enough. A
`#FFFFFFB3` label is an invented opacity rather than a token. Neither is a step
on any ramp. State that is worth showing is worth showing at 3:1.

`deep-blue-200` `#4E6DB5` was the value here until it was measured on pixels
rather than computed: **3.37:1 against the scrolled bar but 2.97:1 against the
at-rest one**, i.e. passing in the state nobody lands on and failing in the
state everybody does. It is the case that produced the rule in
[Measuring contrast on the bar](#measuring-contrast-on-the-bar), and the reason
the active chip now sits one step further up at `deep-blue-100`. Going up the
ramp flips the label: `#6C8AD0` takes dark `on-accent` text, not white — white
on it is only 3.40:1 and the contrast table already pairs it with `#01051D`.

### Mobile drawer

Below 768 the links, the CTA and the locale switcher stack in a drawer that
drops from the bar over a `scrim` `#01051DCC`.

- `surface` `#001751`, opaque — it sits over content, so it cannot be
  translucent. Square top corners (flush with the bar), `2xl` (16px) bottom
  corners, 1px `border-hairline-cool`.
- `20px` padding, matching the bar's side padding so the logo and the first link
  share a left edge. `24px` between groups.
- **`groupGap` is the panel's flex gap, and the separating hairlines are its
  siblings — not its borders.** The panel stacks five children (nav, rule,
  action, rule, switcher) with `24px` between *each adjacent pair*, so the
  distance from the last nav row to the CTA is `24 + 1 + 24 = 49px`, with the
  rule centred in it. It is **not** 24px of total content-to-content space; a
  drawer measuring 24px there has spent the token once instead of twice and
  comes out 48px shorter than the canonical drawing (`V6Joog → gWTnC`).
- Rows are **44px** tall — the touch-target floor — with a `4px` gap. That is
  taller than the 32px product list item on purpose: a thumb is not a cursor.
- Three groups, separated by hairlines and by a **descending type scale**:
  navigation at `nav-lg` (16), the action at `nav-md` (14), the locale switcher
  at `nav-sm` (12). Priority is legible from size alone, before any label is
  read.
- **The action group may hold two actions, and it stays one group.** The sign-up
  link sits above the CTA at `itemHeight` 44 with `itemGap` 4 — 48px of growth,
  no fourth group and no change to `groupGap`. The descending scale ranks
  *groups*, so two items inside one group share `nav-md`; order and chrome rank
  them, primary last. Three groups is the drawer's geometry, not a coincidence:
  a fourth would need a type step below `nav-sm`, and there isn't one.
- Do not wrap the groups in cards. The hairline and the size step already do the
  work, and a container still needs a structural reason to exist.

## Logotype

**The logo is vector in every medium. There is no raster fallback.**

### Where the canonical logo lives

| Medium | Canonical source |
| --- | --- |
| `pencil.pen` | `Logo Vector` frames of **native paths** under `I5xP6 → rPOee` (Page 6 – Logotype) → `sUwc9` (Logo Grid). |
| Code (`app/`) | `public/images/hermes-logo.svg`, inlined or referenced as SVG. |

In the pen, the full lockup is 37 paths at **380 × 86.2** (aspect **4.409:1**);
the short lockup is 33 paths at 172 × 71.4. Pick the variant whose backdrop
matches the surface:

| Frame | Variant | For |
| --- | --- | --- |
| `c01WV` | Navy Full — amber accent | `surface` / `surface-translucent` navy |
| `ab481` | Dark Full — amber accent | `background` `#01051D` |
| `IxO6D` | Navy Full — orange accent | navy, when amber collides with a neighbouring accent |
| `BWjHC` / `Kek1i` | Light Full | light mode, `soft-blue-lm` / white |
| `D8p1RB` / `RtTuI` / `zUYPB` | Short lockup, navy / dark / light | below the 120px minimum |

To place the logo on a new pen screen, **copy one of those frames.** Do not
apply it as an image fill, do not re-import it, and do not draw it.

### `pen-assets/` is not a logo source

`pen-assets/` holds **14 raster `.png` exports** of the logo
(`hermes-logo.png`, `logo-full-*`, `logo-short-*`, `logo-mark-*`, `logo-hdot-*`,
`hermes-mark.png`). They are historical previews, not deliverables, and they are
**not to be used**:

- The project rule is that every logo and illustration is vector. A PNG in an
  image fill is raster by definition and blurs on any HiDPI display or any scale
  other than 1:1.
- pen.dev image fills **do not render `.svg` at all**, so "just swap the fill for
  the SVG" is not available. The only vector path in the pen is native paths.

Any `fill: {type: "image", url: "pen-assets/logo-*"}` or
`.../hermes-logo.png` / `hermes-mark.png` is a defect. Replace it by copying the
matching `Logo Vector` frame.

### Using it

- **Never** recolor, restretch, rotate, crop or rebuild the logo. Sizes are
  chosen by width; the height follows from the locked **4.409:1** aspect. A
  size that does not divide out to 4.409 is a stretch, however small.
- Minimum clear space on all sides equals the height of the "H".
- Minimum full-lockup width **120px**; below that use the short lockup or the
  mark.
- The full lockup contains a counter-shape filled `deep-blue` `#001751`, drawn
  for an opaque navy backdrop. Over a translucent bar or a photograph, check it
  reads as part of the mark and not as a floating block — and if it does not,
  change the *surface*, never the logo.

## Sanctioned exceptions

Deviations from the contract are legal only when they are written **here**. An
exception that is not in this section is a bug and must be corrected on sight.
Each entry names the scope it is confined to; outside that scope the base rule
applies unchanged.

### Marketing navbar CTA — `button-cta-marketing`

**Scope:** the single conversion CTA in the landing navbar
(`app/components/AppNavbar.vue`). Nowhere else — not in the contact modal, not
in pricing, not in the app shell.

**The exception, approved by the user:**

- The button is an **outline**, and the orange is its 1px **stroke**, not its
  fill: `sunset-orange` `#FF734D` border, transparent background, `foreground`
  `#EBF2FF` label. On hover the fill becomes `#FF734D`. The Colors section
  otherwise reserves orange for destructive actions, delays and exceptions.
- Radius is `full` (9999px), although `components.button-*` fixes `rounded.md`
  (6px) for every button variant and the Shapes section reserves `full` for
  pills, badges, avatars and status dots.

**Why it stands.** The landing runs cyan as its dominant accent (nav link hover,
hero). A cyan CTA would dissolve into the accent it shares the bar with; orange
is the only brand accent that reads as a distinct call at that size, and the
pill silhouette separates the conversion action from the structural buttons
inside product UI. This is a deliberate marketing decision, not drift.

**What the exception does *not* license:**

- The text on the fill stays `on-accent` `#01051D` (7.5:1). White on `#FF734D`
  is 2.7:1 and remains forbidden — the contrast table is not part of this
  exception.
- The stroke stays 1px per Elevation. `1.5px` is off the spacing scale and is
  not covered here.
- Label stays `label-lg`. Padding stays `8px 16px`.
- Orange does not become the landing's dominant accent. It appears once, on this
  button, and the 60/30/10 proportion is unchanged.
- It does not license a **second** pill in the bar. "One per bar" is part of the
  scope: the `/signup` route is a text link (`navbar-signup-link`), and if it
  ever has to outrank this button, this section changes first.
- No glow, no colored shadow, no gradient fill on the pill.

**Do not "fix" this button back to `rounded.md` or to a cyan fill.** If it ever
needs to change, change this section first.

### Resolved — the font payload is amended to match the scale

This was recorded as an open contradiction between the contract and the build.
It is now closed, **in favour of the contract**, because the canonical frame is
unambiguous and the cheaper option is also the correct one.

**The evidence.** Page 2.0 of the `Design system` frame (`gx0hd`, "Page 5 -
Typography") sets Headline at **500** (labelled "Medium"), Label at **500**
("Medium | Bold"), Display and Title at **700** ("Bold"), and Body at **400**
("Regular"). The specimen row above the scale shows three faces: Regular, Bold,
Medium. **Weights 300, 600 and 800 appear nowhere in the frame.** The scale in
this document already matches its source; what drifted was `public/fonts/`.

**The decision.** The served set becomes **400 / 500 / 700** — the three weights
the scale actually uses.

- `poppins-500.woff2` is added and declared. Every `font-weight: 500` in this
  contract — the whole Headline and Label tiers, nav items, buttons, form
  labels — then renders as designed for the first time.
- **300 and 800 are dropped.** They are declared, preloaded and served today,
  and no rule in this document and no element in `app/` uses either.
- Preloads follow the same set: 400 and 500, not 400 and 600.

This is not a cost. Serving 400/500/700 is **~15 KB smaller across three files
than the five served today**, so the correct type system is also the faster one.

**600 stays declared, temporarily and only as a migration shim.** 25 call sites
in `app/` still use `font-semibold`. Dropping the face before they are migrated
would resolve them *upward* to 700 and make the landing heavier, not lighter.
The sequence is: ship 500 → migrate each `font-semibold` to its real tier
(`display-*` and `title-*` are 700, `headline-*` is 500) → drop 600 and reclaim
the last ~8 KB.

Until that migration lands, **600 is legal only where it already exists.** It is
not a value to reach for: new work uses 400, 500 or 700, and a nav item, button
or label set to 600 is a defect regardless of which faces happen to be loaded.

## Do's and Don'ts

**Do**

- Start from dark mode; derive light mode from the LM tokens on page 1.1/1.5.
- Pick one accent per screen and let the other two appear only as status.
- Take every accent/text pairing from the contrast table in the Colors section.
- Snap every size, gap and padding to the 4px spacing scale.
- Instance the `K:*` components and override their slots, rather than drawing
  new buttons, inputs or cards.
- Express hierarchy with tonal layers and hairlines before reaching for shadow.
- Keep Poppins as the only family; change size and weight, not typeface.
- Ship a landing section at all five artboard widths before calling it done.
- Keep logos, illustrations and decorative artwork **vector**: native paths in
  `pencil.pen`, SVG in code. Generate them with the pen `Generate(type: "svg")`
  tool or import an existing SVG — never hand-assemble artwork path by path, and
  never fall back to a raster image fill.
- Reuse the logo by copying the canonical `Logo Vector` frame; place a bitmap of
  it nowhere.

**Don't**

- Don't put white text on `blue-sky`, `light-yellow` or `sunset-orange` — bright
  accents always take `#01051D`.
- Don't mix the cool navy surfaces with the warm bronze dashboard surfaces in a
  single screen.
- Don't use cyan, amber and orange together as a gradient or a tri-color fill.
- Don't add neon glows, colored shadows, or blurred backdrops outside the
  sanctioned `Ambient Lights` group.
- Don't invent hexes between the six ramp steps, and don't apply opacity to a
  brand color to fake a tint — use the ramp.
- Don't set body text below 12px, or use bold weight on body copy for emphasis.
- Don't encode state with font weight — not an active nav link, not a selected
  tab, not a current step. State is color plus an ARIA attribute.
- Don't use 13px or 15px anywhere. They are not on any Hermes scale, and a bar
  that only fits at 13px does not fit.
- Don't use the warm `border-hairline` on a navy surface, or the cool one on the
  bronze dashboard; the hairline follows the surface family.
- Don't mark a selected control with a fill under 3:1 against its surroundings —
  WCAG 1.4.11 covers component boundaries, not just text.
- Don't wrap every element in its own card; a container needs a structural
  reason to exist.
- Don't use rounded corners above 16px on structural UI, and never mix radii on
  one frame.
- Don't edit `pencil.pen` with `Read`/`Grep`/`Write` — it is encrypted and only
  the pencil MCP tools may touch it.
